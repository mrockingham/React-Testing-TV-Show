import React from 'react'
import App from './App'
import { render, fireEvent, waitFor, act } from  '@testing-library/react'
import { fetchShow } from './api/fetchShow'

jest.mock('./api/fetchShow')

test('APP Fetches The show data', async () => {
    const mockInfo - {
        
    }
})